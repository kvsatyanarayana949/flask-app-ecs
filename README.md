# Flask App ECS

Simple Flask application for Docker & AWS ECS deployment.

## Endpoints

- `/` → Welcome message
- `/health` → Health check

## Run locally

```bash
pip install -r requirements.txt
python run.py
